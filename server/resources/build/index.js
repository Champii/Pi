// Generated by CoffeeScript 1.7.1
(function() {
  var fs, path;

  fs = require('fs');

  path = require('path');

  exports.mount = function() {
    var basePath;
    basePath = __dirname;
    return fs.readdirSync(basePath).forEach(function(fileName) {
      var filePath, fileStat;
      if (fileName === 'index.coffee') {
        return 0;
      }
      filePath = path.join(basePath, fileName);
      fileStat = fs.statSync(filePath);
      if (fileStat.isFile()) {
        return require(filePath);
      }
    });
  };

}).call(this);
